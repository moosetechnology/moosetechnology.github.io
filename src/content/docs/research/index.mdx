---
title: Research
description: Analyse and model your software system
---

import bibliography from './bibliography.json';

## Our publications

<ul class="list-disc pl-5">
  {bibliography.map((reference) => (
    <li key={reference.title}>
      <strong>{reference.title}</strong>, <i>{reference.event}</i>, {reference.year}.{" "}
      {reference.author.map((author, index) => (
        <span key={index}>
          {author.family} {author.given}
          {index < reference.author.length - 1 && ", "}
        </span>
      ))}
      {reference.URL && (
        <>
          {" "}
          <a href={reference.URL} class="text-blue-500 underline">
            URL
          </a>
        </>
      )}
      {reference.PDF && (
        <>
          {" "}
          <a href={reference.PDF} class="text-blue-500 underline">
            PDF
          </a>
        </>
      )}
    </li>
  ))}
</ul>


